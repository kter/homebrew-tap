# homebrew-tap

Homebrew formulae for [kter](https://github.com/kter)'s tools.

```sh
brew tap kter/tap
brew install zenith-wallpaper
```

Formula versions are bumped automatically by each project's release CI
(see e.g. `zenith-wallpaper`'s `.github/workflows/release.yml`).
